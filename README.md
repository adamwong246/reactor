# reactor
## a lightweight alternative to react-storybook built on esbuild and ts.

### AI Directives
This is a library. The example folder is a stand-in for an app which would import reactor. As such, the library can contain zero references to these examples. 
The list of components should be defined in only 1 place- .storyboardrc.js